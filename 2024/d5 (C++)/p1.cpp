#include <unordered_map>

#include "d5.cpp"

using namespace std;

int main() {
    vector<pair<int, int>> ins;
    vector<vector<int>> upd;

    tie(ins, upd) = readData();

    unordered_map<int, vector<int>> befores;

    return 0;
}